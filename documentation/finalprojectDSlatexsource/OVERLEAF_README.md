# Overleaf Compile Notes

Upload this entire `finalprojectDSlatexsource` folder to Overleaf.

Use `main.tex` as the main document.

Included locally:

- `main.tex`: IEEE conference report source.
- `references.bib`: bibliography entries.
- `IEEEtran.cls`: IEEE class file from the copied template.
- `figures/`: all figures referenced by `main.tex`.

The current report narrative is built around the full notebook sequence through `15_activation_patching.ipynb`. The key causal storyline is direction geometry -> final-layer steering/ablation -> mid-transformer intervention -> same-context activation patching.

The original IEEE sample files are kept for reference:

- `conference_101719.tex`
- `conference_101719.pdf`
- `IEEEtran_HOWTO.pdf`
- `fig1.png`
